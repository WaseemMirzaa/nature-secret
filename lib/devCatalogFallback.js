/** Dev-only empty fallbacks when API returns no catalog (main has no UI mock bundle). */
export function getDevFallbackCategories() {
  return [];
}

export function getDevFallbackProducts() {
  return [];
}
